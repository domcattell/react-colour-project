import { makeStyles, useTheme } from '@material-ui/core/styles';

export default makeStyles(theme => ({
    picker: {
        width: "95% !important",
        marginTop: "2rem"
    },

    addColour: {
        width: "100%",
        padding: "1rem",
        marginTop: "1rem",
        fontSize: "2rem"
    },

    colourInput: {
        width: "100%",
        height: "70px"
    }

}));